# Agent memory (continual learning)

## Learned User Preferences

## Learned Workspace Facts

- This app uses in-memory storage only; DATABASE_URL is not required at runtime (only for Drizzle Kit if used).
